# apps/web — copy this file to apps/web/.env and adjust as needed

# Base URL of the Fastify API (see apps/api). Defaults to http://localhost:4000
# if unset, which matches apps/api's default PORT — this only needs to be set
# explicitly if you're running the API on a different host/port.
VITE_API_BASE_URL=http://localhost:4000
